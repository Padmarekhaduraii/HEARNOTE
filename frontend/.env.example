# HearNote Frontend Environment Configuration
# Point this to the FastAPI backend URL (default: http://127.0.0.1:8001)
VITE_API_URL=http://127.0.0.1:8001

# Set to true only to force offline standalone mock mode
VITE_USE_MOCK=false
