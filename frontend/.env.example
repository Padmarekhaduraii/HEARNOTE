# HearNote Frontend Environment Configuration
# Point this to your teammate's FastAPI backend URL
VITE_API_URL=http://localhost:8000

# Set to true to run in standalone demo/mock mode with realistic data and zero backend dependency
VITE_USE_MOCK=true
